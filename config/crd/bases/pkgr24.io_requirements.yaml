---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: requirements.pkgr24.io
spec:
  group: pkgr24.io
  names:
    kind: Requirements
    listKind: RequirementsList
    plural: requirements
    shortNames:
    - req
    - reqs
    singular: requirements
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Requirements is the Schema for the requirements API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RequirementsSpec defines the desired state of Requirements
            properties:
              requirements:
                description: Foo is an example field of Requirements. Edit requirements_types.go
                  to remove/update
                items:
                  type: string
                type: array
            type: object
          status:
            description: RequirementsStatus defines the observed state of Requirements
            properties:
              bundles:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                items:
                  type: string
                type: array
              message:
                type: string
            required:
            - bundles
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
